# Broccoli DevOps Agent：下一轮打磨清单

整理日期：2026-09-11。目标继续使用原五机环境，主验收实例为 5 号 VPS 上的 Luna / English 控制台。

## 2026-09-11 修复与验收结果

本轮已逐项完成 B-01～B-06，并独立提交既有 runbook 实现。真实 UI 导入验收额外发现并修复 B-07：浏览器重新序列化 JSON 改变浮点表示、大整数精度，导致 artifact 哈希校验失败。

- 已部署到原 5 号 VPS，入口仍为 `http://127.0.0.1:15184/`。
- 后端提交 `88062cb`，前端提交 `9469c63`；后者仅修改 UI 导入，后端二进制保持一致。在线 `deployment-version.json` 给出提交和 SHA-256。
- 保留 Luna / English、独立数据目录、`dry_run=false`、周期采集 `0`，与升级前配置及认证环境逐字节一致。
- 162 项 Rust 测试、6 项前端测试、4 项 Python helper 测试通过；Clippy 与前端生产构建通过。
- 实机完成 worker 停止→Luna 诊断→一次恢复动作→后续快照验证；最终 7/7 Healthy、无 coverage gap、DLQ 为 0。恢复后真实判题三例通过。
- 真实 SSH 断开后 Trace 显示 `not connected`，请求丢失触发 Job 取消；恢复连接后页面自动补齐终态。新版本重启保留配置和数据，无残留运行任务。
- 真实 VPS 会话通过 UI 导入隔离实例，2 轮 Job、1 个动作、7 份 artifact、55 条事件完整保留为只读归档。设置 UI 保存/刷新以及 API 跨重启持久化均通过。

详细提交映射、验收范围与回退步骤：[本轮验收记录](../cloud/audits/2026-09-11-polish/REPORT.zh-CN.md)。以下 A～E 保留最初排期时的问题与验收要求；其中“待提交／待验收”等状态是历史描述，以本节和验收记录为准。

## 最初整理时的基线（历史快照）

- Git：`main@1968090`；之前的 12 项 UX 修复已 squash 合入，不再作为“尚未实现”重复排期。
- 工作区：7 个已跟踪文件有未提交修改，另外 `tests/job_lifecycle.rs`、`tests/operational_runbooks.rs`、`tests/redis_recovery.rs` 尚未跟踪。
- 本次运行了 actions、job_lifecycle、operational_runbooks、redis_recovery 四组测试，**16 项通过**。这不是本次全量工作区检查。
- 历史记录表明，9月9日扩展 runbook 曾在 15184 做过真实模型与实机恢复验证；9月10日生命周期补丁也曾部署并验证。对应代码仍有未提交部分，不能只用 `1968090` 描述这些远端二进制。
- 今天尝试连接 `36.103.236.92:22` 超时；原 SSH 控制连接和本地 15184 监听均不在。**当前线上版本、运行模式、健康状态未实时确认。**

## A. Bugs / fixes

| ID | 优先级 | 问题与证据状态 | 下一步与验收标准 |
|---|---|---|---|
| B-01 | P1 | **观测失败被当成资源故障。** Luna 初次启动缺少探针认证时曾把 worker 显示为 Down、Redis显示为Degraded，且没有对应 coverage gap。启动配置已修正，但 `collector.rs` 的 API 失败仍被包装成 `Ok(ProbeOutcome { succeeded:false })`，由资源聚合逻辑判成 Down/Degraded。 | 区分“目标服务确认异常”和“认证／数据源不可用”。缺凭据、401/403、无法读取心跳数据时应明确给出观测缺口，不能仅据此断言 worker 进程宕机；增加缺凭据、认证失败、上游API不可用、实际缺心跳的对照测试。 |
| B-02 | P1 | **Issue 关闭、请求被丢弃后，Job/待执行操作未一起结束。修复已存在，待入库收口。** 当前补丁加入取消注册、future drop清理、终态Issue准入保护、重启修复。今天5项生命周期测试通过；9月10日记录显示关闭Issue后约0.29秒清空运行状态。 | 审核并独立提交补丁与测试。验证关闭后无新Job、无新动作；迟到回调不恢复已关闭Issue；一个Issue的取消不影响另一个；已开始的机器命令不被误当作未执行。不能据此认定原27分钟等待的所有原因都已解决。 |
| B-03 | P2 | **长等待缺少可解释状态，时间承诺错误。** 曾有Job至少27分钟保持running；当前上报页仍写“最多约一分钟”。有120秒HTTP超时和有限重试，但没有足够现场证据把原异常统一归因于某一处。 | 去掉固定一分钟承诺，展示开始时间、经过时长、最后进展、当前等待/重试阶段；分别验证正常慢请求、超时、取消和断开请求的收尾。根因调查与文案修复分别记录。 |
| B-04 | P2 | **Trace 断流时连接状态可能一直是已连接。源码确认，待现场复现。** `Trace.tsx` 订阅后直接 `setConnected(true)`，没有接入 `streamEvents` 的连接变化回调；轮询兜底又依赖 `!connected`。 | SSE报错时显示断连并启用轮询；重连后恢复实时更新，事件不重复、不丢失。用主动断开/恢复转发验证，不能只验证初次加载。 |
| B-05 | P2 | **上报进度没有按当前请求隔离。源码确认，待并发实测。** `LiveProgress` 只按事件kind筛选全局流，没有Issue/Job/request关联条件。 | 同时运行两个问题时，各提交窗口只显示自己的进度；后台快照审查不能混入当前任务。关联方案需覆盖“提交后尚未返回Job ID”的阶段。 |
| B-06 | P2 | **模型可提议目标没有配置的 verification probe。已有修复草稿。** 当前 `harness.rs` 会逐目标核对快照中的 probe事实，`tests/actions.rs` 新增先拒绝错误探针、再接收正确探针的测试，今天通过。 | 独立提交并回归：不能用Redis的队列探针验证worker；多目标动作的探针须对每个目标可用；错误提议不得排入执行队列；省略自定义探针时仍能使用正常的健康验证。 |

### 交付问题：版本与部署必须收口

当前存在“未提交代码已经以补丁形式在VPS运行过”的历史记录。下一轮首先把生命周期、runbook矩阵、诊断目录、验证探针约束及相关测试拆成可审阅的提交，再从明确提交构建。

验收要求：运行二进制SHA、Git提交、前端资源哈希与部署记录能对应；重启、升级、回退都有明确版本。`1968090 + 某个补丁标签` 不能长期替代可复现提交。

## B. 用户已经明确提出的 feature requirements

以下来自既有对话，已实现的项目作为回归要求保留，不能再次当成待开发新功能。

| ID | 明确要求 | 当前状态 / 本轮验收 |
|---|---|---|
| R-01 | 缺少runbook／命令实现的提议应交给human处理。 | `184a206`已实现：等待人工、保留原因、comment送回上游；缺少实现与权限拒绝、真实执行失败分开。扩展runbook后需回归。 |
| R-02 | Inbox 能反馈、标记Issue已解决；History 要完整，关闭说明可见。 | 入口与历史已实现。生命周期收尾由B-02补齐；要验证“关闭”确实停止后续工作。 |
| R-03 | Trace 按结论→本轮操作→问题事件→完整raw排序，并有时间与合理跳转。 | `1968090`已实现基础UX。保持深链接轮次一致性；本轮补测断流、重连和并发进度。 |
| R-04 | Issues & Jobs 可按问题折叠多轮Job，只展示原问题和最新状况；能够搜索。 | 已实现。保留搜索、展开状态与返回位置的回归；搜索覆盖旧轮次、评论、ID和关闭说明。 |
| R-05 | 用量只统计Mtoks，统一两位小数。 | 已实现。概览、输入/输出/缓存、分模型显示继续保持一致，不把未知用量误当成零。 |
| R-06 | 在原VPS启动独立的gpt-5.6-luna实例，语言为英文，使用独立数据。 | 已启动并修复过探针认证继承。继续以15184为主验收入口；模型密钥与探针凭据分开管理，不在需求文档记录其值。 |

## C. 已有扩展实现，待提交／复核（不冒充新需求）

本地验证材料记录了 **19个配置runbook、其中11个新增**。当前未提交代码包含：

- 诊断：`redis.ping`、`redis.info`、`postgres.check`、`postgres.locks`、`storage.check`、`infra.resources`、`app.health`。
- 恢复：Redis/PostgreSQL的start/restart、存储start/restart，以及既有worker恢复动作。
- 模型读取当前配置中的runbook目录和语义描述；资源类型、参数及权限在执行层再次核对。

历史实机记录包括 Redis、PostgreSQL、存储恢复及恢复后的判题测试；其中动作曾使用 `dry_run=false`。这些是历史证据，不代表今天VPS状态。当前若干源码哈希与那份验证记录一致，但不能把所有未提交改动视为已完成整体验收。

本轮应先版本化并回归这批工作，再决定是否增加更多runbook。

## D. 待验收，不是已确认 bug

| ID | 验收项 | 为什么仍待确认 |
|---|---|---|
| V-01 | 导出→导入→只读归档→Trace完整回放 | HTTP导出曾正常；UI导入失败与连接中断重叠，不能据此判断导入代码坏了。 |
| V-02 | Settings 保存、刷新、重启后的配置一致性 | 上轮未完成完整UI验证；已有后端测试不等于全部页面流程通过。 |
| V-03 | 模型、探针认证、拓扑、数据目录的实例隔离 | Luna 曾漏掉探针认证，已运维修复；重建实例仍需验证不会再次遗漏，也不会串用Sol数据。 |
| V-04 | SSH断连恢复、SSE恢复、长请求取消 | 必须在同一VPS真实验证，分别覆盖可恢复断连与进程重启。 |
| V-05 | DLQ 最终清理状态 | 之前有清理请求及端口转发操作，但此处没有足够证据确认DLQ已经清理。它是运维事项，不自动等价于“新增DLQ删除功能”。 |

## E. 部署目标和执行顺序

继续使用 **5号VPS `36.103.236.92`（内网 `10.0.19.80`）** 作为controller，复用原来的五机拓扑。

- 主验收：Luna / English，页面 `15184 → VPS 5184`，API `14724 → VPS 4724`。
- 对照实例：Sol页面15182；Broccoli业务页面13000。
- Luna配置：`/var/lib/broccoli-agent/validation/luna-1968090/agent.toml`。
- 服务：`broccoli-agent-luna-1968090.service`。名称里含1968090，不足以证明实际二进制就是该提交。
- 先核对实时配置与已授权执行模式；历史材料既有dry-run也有live，不根据旧记录直接覆盖当前模式。

建议顺序：

1. 恢复SSH，核对VPS实际版本、配置和健康；给当前未提交实现做版本收口。
2. 优先完成 B-01、B-02、B-06，保证判断和生命周期正确。
3. 完成 B-03～B-05，保证用户能理解并追踪正在发生什么。
4. 完成 V-01～V-04，再重复一次依赖故障→诊断→受控恢复→after-Snapshot→实际判题的验收。
5. 每项独立commit，验证后再按需要squash到main，在同一VPS部署与复核。

## 证据

- 初始审计：`cloud/audits/2026-09-09-15182/REPORT.zh-CN.md`。
- 12项UX修复：同目录 `FIXES.zh-CN.md`。
- 生命周期实机记录：`cloud/job-lifecycle-validation.json`（2026-09-10）。
- 扩展runbook记录：`cloud/experiments/expanded-playbook-validation-20260909.json` 及其readonly/recovery验证文件。
- 当前源码：`src/collector.rs`、`src/runner.rs`、`src/scheduler.rs`、`src/team/harness.rs`、`web/src/components/Trace.tsx`、`web/src/components/Spend.tsx`。

最初整理清单时仅运行了 16 项定向测试；后续修复、提交与部署结果见本文开头的更新。
