/target/
.DS_Store

# Operator-local state: the file-backed store and deployment-specific topology.
/data/
/config/topology.toml
/config/agent.toml

# Testbed state and build logs. The testbed scripts, its topology, and its agent config
# are committed on purpose -- only what a run produces is ignored.
/data-testbed/
/testbed/.log/
# What `cargo run --example live_demo` and `seed_demo` write.
/data-live/
/data-demo/

# Web console build outputs and dependencies.
/web/node_modules/
/web/dist/
